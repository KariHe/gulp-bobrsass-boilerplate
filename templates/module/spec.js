describe('{{camelCase name}} page', function() {


  beforeEach(module('app.{{camelCase name}}'));

  it('should have value', inject(function({{capitalize name}}) {
    //expect({{capitalize name}}.value).toMatch('test');
  }));
});
